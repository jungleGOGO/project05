<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tsherpa.team35.per.MainphotoMapper">
    <insert id="mainphotoInsert">
        insert into mainphoto(market_no, savefolder, originfile, savefile)
        values
        <foreach collection="mainphotoList" item="mainphoto" separator=" , ">
            (#{marketNo}, #{mainphoto.saveFolder}, #{mainphoto.originFile}, #{mainphoto.saveFile})
        </foreach>
    </insert>
</mapper>